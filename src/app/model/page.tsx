import { PageShell } from "@/components/PageShell";
import { ModelSection } from "@/components/sections/SectionViews";

export default function ModelPage() {
  return (
    <PageShell>
      <ModelSection />
    </PageShell>
  );
}
